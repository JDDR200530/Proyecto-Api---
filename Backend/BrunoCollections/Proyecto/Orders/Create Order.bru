meta {
  name: Create Order
  type: http
  seq: 2
}

post {
  url: {{PATH}}/orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "senderName": "Micky",
    "address": "Copan",
    "receiverName": "Hector"
  }
}
